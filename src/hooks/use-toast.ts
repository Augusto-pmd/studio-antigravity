// This file is being temporarily emptied to reduce dependencies.
// We will restore it after a successful publish.
export function useToast() {
  const toast = () => {};
  return { toasts: [], toast };
}
