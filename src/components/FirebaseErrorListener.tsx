'use client';

// This component has been disabled to prevent a persistent runtime crash.
// The underlying error emitter library had a module compatibility issue.
export function FirebaseErrorListener() {
  return null;
}
